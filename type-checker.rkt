#lang racket/base


(require "source-ast.rkt" "typed-ir-ast.rkt")

(define (type-check prog)

 (define (convert env)
  (define  (recur prog)
   (match prog
    (


  )
  recur)

 (convert (hash) prog))
